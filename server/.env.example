PORT=5000
# Frontend origin. Use the exact HTTPS production URL, without a trailing slash.
CLIENT_URL=http://localhost:5173
DB_HOST=localhost
DB_PORT=3306
DB_NAME=attendance_system
DB_USER=root
DB_PASSWORD=
# Use a long random value in production (at least 32 characters).
JWT_SECRET=change-this-secret-in-development
FACE_MATCH_THRESHOLD=0.6
